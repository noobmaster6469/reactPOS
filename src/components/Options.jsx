import { aside, div } from "motion/react-client";
import React from "react";

const Options = () => {
  return (
    <div className="hidden md:flex flex-col gap-2">
      <button className="btn text-xl">Admin</button>
      <button className="btn text-xl">Admin</button>
    </div>
  );
};

export default Options;
